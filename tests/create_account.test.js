/* 
 * Contact: team@fusioncode.ai
 *
 *
 * Project  - create account UNIT TESTS
 * Application Client
 * MonDos
*/

/* the create account view has a back icon located at the very-top left of the view which on click will trigger the back() action */
it("the create account view has a back icon located at the very-top left of the view which on click will trigger the back() action", () => {
  /* ToDo developer needs to complete unit test 
               the create account view has a back icon located at the very-top left of the view which on click will trigger the back() action
               uncomment below and update the code to test
                //instance.action(function(error) {
                //  if (error) throw error;
                //expect().to.be();
                //});
                done();
               */
})
